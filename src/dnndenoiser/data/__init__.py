"""Synthetic spectrum generation.

Import from ``dnndenoiser.data.synthetic_generator``; the generator and its
configuration objects are also re-exported at the package top level.

``dnndenoiser.data.frame_stack`` holds the HDF5 layout for a stack of repeated
acquisitions, which the self-supervised moving-average method trains from.
"""
